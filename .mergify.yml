pull_request_rules:
- actions:
    queue:
      name: default
      priority: high
  conditions:
  - base=20221206150627/test_batch_max_failure_resolution_attempts/main
  - label=queue
  name: Merge priority high
queue_rules:
- batch_max_failure_resolution_attempts: 0
  batch_max_wait_time: 0 s
  batch_size: 4
  conditions:
  - status-success=continuous-integration/fake-ci
  name: default
