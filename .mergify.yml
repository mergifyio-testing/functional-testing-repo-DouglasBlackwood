pull_request_rules:
- actions:
    queue:
      name: default
      priority: high
      update_method: rebase
  conditions:
  - base=20220715121304/test_queue_update_inplace_blocking_merge/main
  - label=queue
  name: Merge priority high
queue_rules:
- conditions:
  - status-success=continuous-integration/fake-ci
  name: default
